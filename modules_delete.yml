info:
  name: modules_delete
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{url}}/api/modules/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
